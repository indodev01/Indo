IndoDev frontend asset structure.

Root-level JavaScript lives in `js/`, root-level CSS lives in `css/`, and root-level HTML pages live in `html/`.
Feature-specific folders such as `auth/`, `dashboard/`, `landing/`, and `app/` retain their local page assets.
